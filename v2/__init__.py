"""
PROJECT-ALPHA V2 — Event-driven trading infrastructure.

V1 (bots/, app.py) is frozen and runs on port 5000.
V2 runs alongside on port 5001, progressively taking over services.

Phase: V2.1 — Scanner bridge + SQLite + Event Bus + API
"""

__version__ = "2.1.0"
